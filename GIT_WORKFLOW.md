echo "# Git Workflow Documentation

## Commands Used
- git init, config, add, commit, push, pull
- git branch, checkout -b, merge
- git tag

## Branching Strategy
main <- dev <- feature branches

## Pull Request Process
1. feature -> dev PR
2. dev -> main PR

## Tags
v1.0.0 - First stable release

## .gitignore
Excludes logs, temp files, OS metadata, secrets" > GIT_WORKFLOW.md